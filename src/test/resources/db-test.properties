# Test DB properties - edit to match your MySQL server credentials
# This file is used by tests. The test harness will create/drop database `employeeData_test` on the server URL below.
# Example server URL (no default database): jdbc:mysql://localhost:3306/?serverTimezone=UTC&useSSL=false
# Example db.url (database to use): jdbc:mysql://localhost:3306/employeeData_test?serverTimezone=UTC&useSSL=false

# Server URL used for create/drop operations (must not include a database path)
db.serverUrl=jdbc:mysql://localhost:3306/?serverTimezone=UTC&useSSL=false

# Test database URL (this database will be created/dropped by the tests)
db.url=jdbc:mysql://localhost:3306/employeeData_test?serverTimezone=UTC&useSSL=false

db.user=root
# db.password=yourpassword
db.password=
